import styled from 'styled-components';

import { Button, Typography } from 'antd';

export const StyledButton = styled(Button)`
    margin-left: auto;
    display: flex;
    align-items: center;
    font-size: 20px;
`;

export const StyledListHeader = styled(Typography.Text)`
    font-size: 20px;
`;
