import styled from 'styled-components';
import { Typography } from 'antd';

export const StyledTopicTitle = styled(Typography.Text)`
    font-size: 20px;
    font-weight: 700;
`;

export const StyledWrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 16px;
`;
