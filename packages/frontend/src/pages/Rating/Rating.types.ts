// import { IUser } from "../../models/Entity/User";

export type TRating = {
    data: {
        id: number;
        name: string;
        avatar: string;
        level: number;
        score: number;
        teamName: string;
    };
};
